package org.example.Helpers;

// Used to give structure for Helper class
public interface Helpers {
    int randomAttackMonster();
    int randomAttackHero();
    int randomPercentage();
    void sleepForMilliSeconds(int time);
}
